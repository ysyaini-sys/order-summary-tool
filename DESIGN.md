# 银灰工作台视觉规范

本次依据用户确认的银灰波纹视觉稿实现。页面仍使用现有静态 HTML 与 GitHub Pages，不引入框架或新的运行时依赖。

## 视觉与功能范围

- 首页：轻量导航、银灰波纹标题区、四个独立上传卡片、可滚动结果表格。
- 千川页：相同导航与标题区，文件导入紧接标题，筛选、统计、账户对比和明细沿用现有数据。
- 画稿中的虚构环比、趋势、商品图片和额外导航不加入生产页面。
- 千川入口保持新标签页打开，导出 HTML 报告内嵌完整样式且移除工作台导航，离线阅读不依赖本站资源。
- 760px 以下单列布局；按钮与链接支持可见键盘焦点。

## 与订单开发协作

`index.html` 和 `qianchuan.html` 中的 `<style id="silver-workspace-theme">` 为本次主题。主题使用内嵌 SVG 绘制波纹，不请求外部图片。为保证下载报告脱离网站后仍有完整样式，主题内嵌在 HTML 中。

调整订单业务时，请以最新 main 为基础，只修改必要段落；保留主题块、workspace-nav、workspace-tabs 和千川入口。所有业务元素的原 ID 已保留。不要使用旧 HTML 整页替换，提交通过独立分支与 PR 审查。

## 验证

`node --test tests/*.test.js`：8 项通过。订单页所有脚本与基础版本一致；千川仅在导出时增加移除导航的操作，分析脚本不变。人工验收需覆盖桌面及手机显示、文件选取、汇总、千川筛选、报告导出和离线打开。
