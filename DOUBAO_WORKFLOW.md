# 豆包协作规范

## 你的角色

你负责在 `order-summary-tool` 仓库中完成视觉设计、页面样式和经过确认的局部页面功能。Codex 是主架构维护者，负责整体结构、数据流程、核心业务逻辑、加密文件读取和跨页面接口。

## 必须遵守的规则

1. 不直接修改 `main` 分支。
2. 每项工作使用独立分支，例如：
   - `feature/doubao-style`
   - `feature/doubao-qianchuan-ui`
   - `fix/doubao-layout`
3. 开始修改前，先确认当前分支和目标文件。
4. 不覆盖、不删除以下核心文件中的业务逻辑：
   - `index.html` 中的订单读取、日期识别、汇总和导出逻辑
   - `qianchuan.js` 中的数据分析逻辑
   - 快手加密文件读取和解密逻辑
5. 如果必须修改 `index.html`，只修改页面入口、样式或明确指定的区域，不整文件替换。
6. 不把 Excel、CSV、密码、测试数据或本地路径提交到仓库。
7. 不引入后端、抓取功能、自动下载功能或新的账号权限，除非用户和 Codex 明确要求。
8. 完成后必须创建 PR，说明修改了哪些文件、如何测试、是否有风险。
9. 只有用户确认测试通过后，才能合并 PR。

## 文件职责

| 文件 | 责任边界 |
|---|---|
| `index.html` | 订单汇总入口、平台上传区、公共页面样式 |
| `qianchuan.html` | 千川分析页面和展示布局 |
| `qianchuan.js` | 千川数据读取、分析和报告逻辑，不能随意重写 |
| `sheet.html` | 在线表格展示和导出 |
| `tests/` | 分析逻辑和页面行为测试 |
| `docs/` | 架构、协作和发布说明 |

## 与 Codex 的配合方式

- Codex 修改核心逻辑时，你不要同时修改同一段代码。
- 你需要修改同一文件时，先在 PR 描述中说明具体行或区域。
- 如果发现 Codex 已经修改了相关区域，保留 Codex 的逻辑，在其上做局部样式或布局调整。
- 不要把 Codex 分支的代码复制进你的分支，也不要用旧版 `index.html` 覆盖新版本。
- 发生冲突时暂停合并，把冲突文件和冲突区域告诉用户，由 Codex 负责判断业务逻辑。

## PR 描述模板

```md
## 做了什么

- 

## 修改文件

- 

## 测试方式

- [ ] 首页可以打开
- [ ] 订单上传入口正常
- [ ] 千川分析入口正常
- [ ] 千川分析弹窗或页面正常
- [ ] 下载/在线表格按钮正常
- [ ] 未修改核心数据处理逻辑

## 风险和注意事项

- 
```

## 合并前要求

用户先在 PR 的预览版本中测试。用户确认后再合并到 `main`。合并后等待 GitHub Pages 发布完成，再检查正式网址。
