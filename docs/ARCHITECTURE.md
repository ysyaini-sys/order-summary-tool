# 工作站与网站架构

## 工作站角色

- Mac：本地数据处理、代码编辑、预览和备份。
- Codex：主架构、核心业务逻辑、测试、故障修复和发布准备。
- 豆包：视觉、页面样式和经过确认的局部界面修改。
- GitHub：代码唯一来源、分支隔离、PR 审核和历史回退。
- GitHub Pages：只发布 `main` 分支的正式版本。

## 仓库结构

```text
main
├── index.html       订单汇总入口
├── qianchuan.html   千川分析独立页面
├── qianchuan.js     千川分析业务逻辑
├── sheet.html       在线表格页面
├── tests/            测试
└── docs/             架构和协作说明
```

## 数据流

```text
本地 Excel/CSV
      ↓
浏览器文件选择
      ↓
SheetJS 或页面解析逻辑
      ↓
浏览器内存中的分析结果
      ↓
页面预览 / 下载 / 在线表格
```

原始文件和密码不上传服务器，也不提交到 GitHub。

## 分支流

```text
main  ←  PR  ←  Codex 分支
  ↑
  └──── PR  ←  豆包分支
```

`main` 是唯一正式版本。任何未经过用户测试的改动都不能进入 `main`。
